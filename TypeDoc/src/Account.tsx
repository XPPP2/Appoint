/**
 * Delete the selected account.
 * @param User
 */
import {User} from "./User";

export function Delete(param: User): void {

}

/**
 * search for account by name, then output on the UI view.
 * @param name Provide the user's name
 */
export function queryAccount(param: string): void {

}

