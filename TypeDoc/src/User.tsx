/**
 * The information of user.
 */
export interface User {
    /** User Name for login. */
    username: string;
    /** Real name. */
    name: string;
    /** Password for login. */
    password: string;
    /** Phone number. */
    phone: string;
    /** Email of user. */
    email: string;
}


/**
 * Find doctor information in database, show the doctor list.
 * @param username The doctor information.
 * @param password The doctor information.
 */
export function Login(): void {

}

/**
 * Get the string information, then make change in database.
 */
export function Register(param: string): void {

}







